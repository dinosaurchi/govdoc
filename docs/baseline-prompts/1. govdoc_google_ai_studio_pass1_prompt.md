# GovDoc SecureFlow — Google AI Studio Pass 1 Prompt

Use this prompt **as the actual instruction text** in Google AI Studio.

## What to attach as context
Attach these files as reference context before sending the prompt:
- `govdoc_source_of_truth_plan_v1_1.md`
- `govdoc_implementation_plan.md`
- optionally: `govdoc_google_ai_studio_prompt_plan.md`

Then paste the prompt below as the message.

---

## Pass 1 prompt

```text
You are generating the **Pass 1 baseline architecture** for GovDoc SecureFlow.

Read the attached context files first, but follow the instructions in this prompt as the highest-priority implementation task.

We are building **GovDoc SecureFlow**, a public-sector MVP for:
- incoming administrative document intake and triage
- no real login
- role switcher only: Intake Clerk, Department Reviewer, Consultant, Supervisor
- hero taxonomy: công văn, quyết định, thông báo, tờ trình, báo cáo
- workflow states:
  - intake_received
  - registered
  - routed_pending_human_review
  - assigned_to_department
  - under_review
  - consultation_requested
  - consultation_completed
  - response_prepared
  - closed
  - archived_demo_only
- must show:
  - upload/intake
  - document detail
  - AI analysis panel
  - review/routing approval
  - consultation thread
  - tracking dashboard
  - closeout/response view
- only core/business logic may be mocked
- mocks must be separated from controllers/routes and easy to replace later
- fail fast
- no hidden errors
- no fake success states
- docker-compose + Makefile required
- backend = FastAPI/Python
- frontend = Next.js/TypeScript
- SQLite for baseline persistence
- app must look production-ready in structure and UI, even if AI internals are mocked

Locked stack and architecture:
- Frontend: Next.js 15, TypeScript, App Router, Tailwind CSS
- Backend: FastAPI, Python 3.12, Pydantic, SQLAlchemy, Alembic
- Persistence: SQLite
- Deployment/dev workflow: docker-compose, Makefile
- Separate `web` and `api` services
- The repo must clearly separate:
  - frontend UI
  - backend API
  - deterministic workflow/state logic
  - AI integration adapter layer
  - core/business modules
  - storage/extraction modules
  - seeded demo scenario data
  - tests

The exact folder names can vary slightly, but the repo must clearly support responsibilities equivalent to:

repo/
  web/
  api/
  deploy/
  scripts/
  prompts/
  tests/
  data/

Recommended structure:

repo/
  web/
    app/
    components/
    lib/
    hooks/
    types/
  api/
    app/
      api/
      core/
      db/
      models/
      schemas/
      services/
      repositories/
      workflows/
      adapters/
      demo/
    tests/
  deploy/
    docker-compose.yml
    Dockerfiles...
  scripts/
    seed_demo_data.*
    qa_local.*
    check_credentials.*
  data/
    demo_scenarios/
    fixtures/
  Makefile
  .env.example
  README.md

Mock module placement rule:
The baseline must make it easy to replace mocks later.

Recommended module shape:

api/app/services/ai/
  interface.py
  mock_provider.py
  future_modelstudio_provider.py

api/app/services/extraction/
  interface.py
  mock_extractor.py
  future_pdf_extractor.py
  future_ocr_provider.py

api/app/services/retrieval/
  interface.py
  mock_retriever.py
  future_embedding_retriever.py

Controllers/routes must depend on interfaces or service facades, not on mock implementations directly.

Pass 1 goal:
Create the **correct production-style scaffold** for the repo and app structure, with the right entities, screens, routes, and persistence skeleton, but without implementing the real core/business logic yet.

Pass 1 must produce:
- monorepo-like structure with `web` + `api`
- Next.js frontend scaffold with real routes/pages/layout
- FastAPI backend scaffold with real routers/models/schemas
- SQLite setup + migrations scaffold
- source-of-truth-aligned entities
- role switcher UI skeleton
- scene/page skeletons
- health endpoints
- `.env.example`
- Dockerfiles + docker-compose scaffold
- Makefile scaffold
- mock service interfaces/facades created, but logic can still be placeholder
- README with run instructions

Pass 1 must not do:
- do not implement real AI logic
- do not implement real extraction/OCR
- do not implement retrieval
- do not implement elaborate demo behavior
- do not fake everything in frontend-only state
- do not hide backend behind a stubbed single endpoint
- do not add real auth
- do not rewrite the chosen stack
- do not collapse backend into frontend
- do not produce a generic chat app
- do not skip the backend entity structure

Requirements for this pass:

1. Create a real baseline repo structure with:
   - `web` = Next.js 15 + TypeScript + Tailwind
   - `api` = FastAPI + Python 3.12 + SQLAlchemy + Alembic + Pydantic
   - SQLite for baseline persistence
   - Dockerfiles + docker-compose
   - Makefile scaffold
   - `.env.example`
   - README with local boot instructions

2. The frontend must already contain real routes/pages for:
   - home / role switcher
   - intake queue
   - record detail / AI analysis
   - review / routing approval
   - consultation thread
   - tracking dashboard
   - response / closeout

3. The backend must already contain real entity/schema/API structure for:
   - document
   - document_file
   - extracted_artifact
   - ai_analysis
   - routing_decision
   - consultation_note
   - audit_event
   - role
   - department
   - demo_scenario
   - prompt_version

4. The backend must already have a workflow/state-machine layer and service boundaries, but the actual business logic may still be placeholder for now.

5. Create explicit interface/facade layers for mock-replaceable modules:
   - AI provider
   - extraction provider
   - retrieval provider
   - workflow service
   These must be separated so later coding agents can implement real logic without changing controllers/routes.

6. No real login. Role switcher only.

7. Use fail-fast config loading and add `/health` endpoint(s).

8. Do not implement the real business logic yet. The goal of this pass is correct structure, contracts, pages, entities, config, and deploy scaffold.

Output requirements:
- create/update the codebase
- output a concise summary only, not a long essay
- include:
  - final repo tree
  - files created/modified
  - assumptions made
  - what remains intentionally mocked after this pass

Important:
- prefer correct structure over feature completeness
- if a design choice is unclear, choose the option that makes later coding-agent replacement easier
- do not embed mock business logic directly in routes/controllers/components
- do not hide missing pieces; mark them clearly with TODOs if needed
```

---

## Quick review checklist after AI Studio finishes

Check these before moving to Pass 2:
- `web` and `api` both exist
- Next.js + FastAPI stack is correct
- SQLite + Alembic scaffold exists
- scene/page structure matches the required flow
- backend entities exist and are aligned
- role switcher exists
- no real auth was added
- mock service interfaces are separated from routes/controllers
- docker-compose, Makefile, `.env.example`, and README exist
- health endpoint exists
