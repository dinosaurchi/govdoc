# GovDoc SecureFlow — Next Step After Pass 2.5 Review

Version: 1.0  
Purpose: Decide whether the current baseline is ready for Pass 3, and provide the exact next Google AI Studio prompt.

---

## Verdict

**Go to Pass 3 now.**

The repo is now **good enough to move forward**.

Why it is ready enough:
- intake is now a real baseline file-upload path instead of a simulated JSON-only create flow
- `document_file` rows are persisted and surfaced in the document detail page
- `extracted_artifact` rows are persisted and surfaced in the document detail page
- workflow state mutations are writing audit events through the workflow/audit services
- the main baseline product flow now feels coherent enough for a real MVP scaffold
- the previous root-level frontend structure noise is gone; the repo now has one clear `web/` frontend app structure

That means the repo has crossed the threshold from:
- “still missing key pass-2 baseline behavior”

to
- “working baseline app that is ready for developer-workflow/deployment hardening in Pass 3”.

---

## What improved in Pass 2.5

The following baseline gaps were materially fixed:

1. **Real upload path exists**
   - the intake page now uploads a real file to `/documents/upload`
   - backend validates file upload and persists it

2. **`document_file` is now meaningfully used**
   - filename, mime type, size, storage path, and hash are persisted
   - detail page now shows file metadata from the backend

3. **`extracted_artifact` is now meaningfully used**
   - extraction output is stored and tied to the uploaded document/file
   - detail page now shows extracted text and structured metadata preview

4. **Audit logging is more credible now**
   - upload/intake and workflow mutations write audit events through the audit service

5. **Repo structure is cleaner**
   - stray/conflicting root-level frontend folders are gone
   - the repo now has one clear frontend app structure

---

## What is still imperfect (but no longer blocking)

These issues still exist, but they are appropriate for Pass 3 rather than forcing another corrective Pass 2.x:

1. **Makefile is still too thin**
   - missing many targets expected by the later implementation plan
   - this is exactly what Pass 3 is supposed to harden

2. **README is outdated/thin**
   - still references earlier pass status
   - does not yet explain the current baseline clearly enough
   - Pass 3 should fix this

3. **Docker/dev workflow is not hardened enough yet**
   - compose/bootstrap exists, but the repo is not yet at the implementation-plan developer workflow level
   - Pass 3 should address this

4. **Some endpoints remain baseline-level and mocked by design**
   - that is acceptable, because Pass 3 is not about real Model Studio logic yet

So the repo is **not finished**, but it is now **ready enough for Pass 3**.

---

## Recommendation

Move to **Pass 3**.

Pass 3 should now focus on:
- Makefile hardening
- docker-compose/dev workflow hardening
- local QA scripts
- honest `check-credentials` scaffolding or implementation
- remote deploy scaffold for `up-remote`
- test scaffolding
- README / env / demo-mode documentation cleanup

Do **not** use Pass 3 to re-open large product-baseline work unless something is clearly broken.

---

## Exact prompt for the next step

Attach:
- `govdoc_source_of_truth_plan_v1_1.md`
- `govdoc_implementation_plan.md`
- optionally: `govdoc_google_ai_studio_prompt_plan.md`

Then paste this prompt.

---

```text
You are doing **Pass 3** for GovDoc SecureFlow.

Read the attached context files first, but follow this prompt as the implementation task.

Locked stack:
- Frontend: Next.js 15 + TypeScript + App Router + Tailwind
- Backend: FastAPI + Python 3.12 + Pydantic + SQLAlchemy + Alembic
- Persistence: SQLite
- Deployment/dev workflow: docker-compose + Makefile
- No real auth
- Role switcher only
- Core/business logic must still remain mocked and isolated where not yet implemented

GovDoc SecureFlow product shape:
- incoming administrative document intake and triage
- hero taxonomy: công văn, quyết định, thông báo, tờ trình, báo cáo
- workflow states:
  - intake_received
  - registered
  - routed_pending_human_review
  - assigned_to_department
  - under_review
  - consultation_requested
  - consultation_completed
  - response_prepared
  - closed
  - archived_demo_only
- fail fast
- no hidden errors
- no fake success states

Current repo status:
- the baseline app is now working well enough
- Pass 3 must harden the developer workflow, deployment scaffold, QA scaffold, and docs
- do not rewrite the product structure
- do not rewrite the chosen stack

Your task in this pass is to harden the baseline repo for later coding-agent implementation.

Requirements for this pass:

1. Finish the developer workflow and deployment scaffold.
   Implement or tighten these Makefile targets:
   - lint
   - build
   - test
   - ci
   - check-credentials
   - up
   - down
   - logs
   - qa
   - up-remote
   They must be honest and fail fast.

2. Tighten docker-compose and local boot flow.
   - ensure web + api boot coherently
   - ensure health checks exist and are usable
   - do not pretend services are healthy if they are not

3. Implement scripts/helpers for:
   - seed demo data
   - local QA smoke check
   - check-credentials
   - remote deployment packaging flow
   Put them in clear script locations and wire them into the Makefile where appropriate.

4. `make check-credentials`:
   - validate required env vars
   - if a minimal real Model Studio credential check can be implemented safely, do it
   - if not, create a clearly isolated adapter + TODO and make the command fail explicitly with a clear “not implemented yet” message instead of pretending success
   - do not create fake success behavior

5. `make up-remote`:
   - create the scaffold/contract expected by the implementation plan
   - package Docker images
   - prepare rsync-based remote deployment flow
   - do not assume Alibaba-specific host features
   - if any piece is placeholder, it must fail explicitly and honestly

6. Add test scaffolding aligned with later work.
   - unit test layout
   - integration test layout
   - e2e test layout placeholder
   - keep it lightweight but structurally ready for later implementation

7. Tighten README and docs.
   README must clearly describe:
   - how to run locally
   - required env vars
   - demo mode / seeded scenarios
   - what is real vs mocked
   - what later coding agents are expected to replace
   - current pass status
   Remove outdated statements from earlier pass reviews.

8. Preserve the current baseline behavior.
   - do not break upload flow
   - do not remove persisted file/artifact behavior
   - do not collapse mock logic into routes/controllers
   - do not rewrite the frontend structure

9. Output requirements:
   - modify the codebase
   - output only a concise summary
   - include:
     - files changed
     - final Makefile targets
     - final docker-compose services
     - current mocked modules still intentionally pending later implementation
     - any remaining explicit TODOs
     - whether the repo is ready for later coding-agent implementation passes

Important:
- do not implement real Model Studio business logic yet
- do not implement real OCR yet
- do not add auth
- do not hide placeholders as if they are finished
- do not create fake success behavior in `check-credentials`, `qa`, or deployment commands
- prefer honest, deterministic developer workflow over broad scope
```

---

## Review checklist after Pass 3

Only move on if all of these are true:

- `make up` exists and is coherent
- `make down` exists
- `make logs` exists
- `make lint`, `make build`, `make test`, `make ci` exist and behave coherently
- `make qa` exists and is honest
- `make check-credentials` exists and either works for real or fails explicitly and honestly
- `make up-remote` exists and is honest about what is implemented vs pending
- README clearly separates real baseline behavior from mocked core/business modules
- repo is ready for later coding-agent implementation without structural rework

---

## Short recommendation

**Next step = Pass 3.**  
The baseline is now good enough; the right next move is developer-workflow and deployment hardening.
