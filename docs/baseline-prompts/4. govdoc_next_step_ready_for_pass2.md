# GovDoc SecureFlow — Next Step After Latest Pass 1 Review

Version: 1.0  
Purpose: Decide whether the current corrected baseline is ready for Pass 2, and provide the exact next Google AI Studio prompt.

---

## Verdict

**Go to Pass 2 now.**

The baseline is now structurally good enough to move forward.

Why it is ready enough:
- backend entity coverage is materially expanded
- migration scaffold is structurally real
- route groups exist for the core baseline areas
- repository/workflow scaffolding exists
- backend role/policy scaffold exists
- retrieval is now separated from extraction
- documents API is no longer just trivial inline hardcoded returns

That means the repo has crossed the threshold from:
- “too loose / too noisy for pass 2”
to
- “good enough to start making the baseline actually work”

---

## What is still imperfect (but no longer blocking)

These issues still exist, but they can be folded into Pass 2 instead of forcing another corrective pass:

1. **Stray root-level frontend folders still exist**
   - `app/`
   - `hooks/`
   - `lib/`
   outside the intended `web/` structure

   These should be cleaned as part of Pass 2 housekeeping.

2. **Some frontend pages are still too hardcoded**
   - especially detail-style pages and some UI states

   Pass 2 is exactly where they should become API-backed.

3. **Makefile / README / env shape are still thin**
   That is acceptable for now.
   Pass 3 is the proper place to harden the developer workflow and deployment scaffolding.

So the repo is **not perfect**, but it is now **ready enough for Pass 2**.

---

## Recommendation

Move to **Pass 2**.

Pass 2 should:
- make the frontend pages real and connected to backend APIs
- make backend persistence actually used
- add seeded demo scenarios
- keep core/business logic mocked behind interfaces
- clean the remaining repo/frontend noise while doing so

---

## Exact prompt for the next step

Attach:
- `govdoc_source_of_truth_plan_v1_1.md`
- `govdoc_implementation_plan.md`
- optionally: `govdoc_google_ai_studio_prompt_plan.md`

Then paste this prompt.

---

```text
You are doing **Pass 2** for GovDoc SecureFlow.

Read the attached context files first, but follow this prompt as the implementation task.

Locked stack:
- Frontend: Next.js 15 + TypeScript + App Router + Tailwind
- Backend: FastAPI + Python 3.12 + Pydantic + SQLAlchemy + Alembic
- Persistence: SQLite
- Deployment/dev workflow: docker-compose + Makefile
- No real auth
- Role switcher only
- Core/business logic must still remain mocked and isolated

GovDoc SecureFlow product shape:
- incoming administrative document intake and triage
- hero taxonomy: công văn, quyết định, thông báo, tờ trình, báo cáo
- workflow states:
  - intake_received
  - registered
  - routed_pending_human_review
  - assigned_to_department
  - under_review
  - consultation_requested
  - consultation_completed
  - response_prepared
  - closed
  - archived_demo_only
- must show:
  - upload/intake
  - document detail
  - AI analysis panel
  - review/routing approval
  - consultation thread
  - tracking dashboard
  - response / closeout
- fail fast
- no hidden errors
- no fake success states

Current repo status:
- baseline structure is now good enough to proceed
- this pass must turn the scaffold into a **real working baseline**
- core/business logic must still stay mocked behind interfaces

Your task in this pass is to convert the scaffold into a **working baseline app** with real frontend/backend behavior, real persistence, seeded demo scenarios, and isolated mock core/business modules.

Requirements for this pass:

1. Make the frontend pages real and API-backed.
   - queue/list pages should load from backend APIs
   - detail page should load a real document record from backend
   - review page should load real records and actions from backend
   - consultation page should load real consultation thread data
   - dashboard should load real backend stats endpoints
   - response/closeout page should use backend-backed state
   - role switcher should affect UI actions and backend requests

2. Make backend persistence actually used.
   - documents
   - document files
   - extracted artifacts
   - AI analysis records
   - routing decisions
   - consultation notes
   - audit events
   - roles
   - departments
   - demo scenarios
   should all be stored and queryable through SQLite where appropriate for baseline behavior

3. Add real baseline API routes for:
   - list documents
   - get document detail
   - create/upload document baseline record
   - load seeded demo scenarios
   - run mock analysis
   - approve / reroute / escalate / request consultation
   - fetch consultation thread
   - fetch dashboard stats
   - update closeout / response-prepared state

4. Implement a real workflow state progression in backend storage.
   - still keep AI/business internals mocked
   - state changes must write audit events
   - invalid transitions must fail explicitly

5. Keep all core/business logic mocked behind isolated modules/files:
   - AI classification/summarization/routing
   - extraction
   - retrieval/evidence
   - consultation auto-response if needed
   These mocks must return realistic structured payloads aligned with future contracts.

6. Add seeded demo scenarios for:
   - hero flow
   - ambiguity flow
   - scan flow
   - out-of-scope flow
   Seed them in a way that later coding agents can replace or extend.

7. Add realistic frontend states:
   - loading
   - empty
   - error
   - success
   and make errors visible instead of hidden.

8. Clean remaining repo/frontend structural noise while doing this pass:
   - remove or reconcile stray root-level frontend folders like `app/`, `hooks/`, `lib/` if they are not truly required
   - keep one clear frontend app structure under `web/`

9. Preserve future-replaceable architecture.
   - routes/controllers must not embed mock business logic directly
   - service boundaries must remain clean
   - do not collapse backend logic into the frontend

10. Output requirements:
   - modify the codebase
   - output only a concise summary
   - include:
     - files changed
     - key routes added or updated
     - key API-backed pages completed
     - key mock modules still intentionally mocked
     - what should happen next before Pass 3

Important:
- do not implement real Model Studio calls yet
- do not implement real OCR yet
- do not implement real embeddings/rerank yet
- do not add auth
- do not bypass backend persistence with frontend-only fake state
- do not hide placeholders as if they are real
- prefer a stable baseline over broad scope
```

---

## Review checklist after Pass 2

Only move forward if all of these are true:

- frontend pages are API-backed instead of mostly hardcoded
- document detail page uses real backend data
- review / consultation / dashboard / response views hit real API endpoints
- workflow state changes persist
- audit events are written on mutations
- seeded demo scenarios exist
- role switcher affects UI + backend requests
- mock business logic remains isolated in separate modules/files
- stray root frontend folders are gone or clearly reconciled
- repo still preserves the locked stack and future-replaceable structure

---

## Short recommendation

**Next step = Pass 2.**  
This is now the right time to turn the scaffold into a real working baseline.
