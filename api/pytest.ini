[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: Fast, isolated unit tests
    integration: Tests that exercise the app, DB, or HTTP stack
    e2e: Reserved for Playwright browser tests (not run in default CI)
